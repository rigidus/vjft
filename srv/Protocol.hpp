// Protocol.hpp
#ifndef PROTOCOL_HPP
#define PROTOCOL_HPP

#include <cstddef>

const std::size_t MAX_NICKNAME = 20;
const std::size_t MAX_IP_PACK_SIZE = 16384; // TODO: remove it, use vectors
const std::size_t PADDING = 4;

#endif // PROTOCOL_HPP
